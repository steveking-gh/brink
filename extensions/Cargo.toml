[package]
name = "extensions"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[features]
std-crc32c = ["dep:std_crc32c"]
std-sha256  = ["dep:std_sha256"]

[dependencies]
ext        = { path = "../ext" }
std_crc32c = { path = "../std/crc32c",   optional = true }
std_sha256  = { path = "../std/sha256",  optional = true }
