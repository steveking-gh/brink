// Named argument to firmion::test_crc: value=42.
// Semantically identical to the positional form firmion::test_crc(42).
// Expected output: 0x00 0x00 0x00 0x2A (big-endian u32).
section top {
    wr firmion::test_crc(value=42);
}
output top;
