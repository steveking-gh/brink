section bar {
    wrs "bar";
}

// top level section
section foo {
    set_addr 0x1000;
    wr bar;
    wr boo;
}

// section after reference
section boo {
    wrs "boo";
    // should fail since bar isn't in this section
    assert sec_offset(bar) == 0;
}

output foo;