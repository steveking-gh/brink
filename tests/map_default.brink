// Integration test source for --map-hf default filename derivation.
// When --map-hf is given without a filename, brink creates map_hf_default.map.txt.
section foo {
    set_addr 0x1000;
    wrs "map";
}

output foo;
