// Passes an integer followed by a string const as extension arguments.
// IRDB accepts both; firmion::test_sum8 rejects the string at runtime with ERR_167.
const LABEL = "world";
section top {
    wr firmion::test_sum8(1, 2, 3, 4, 5, 6, 7, LABEL);
}
output top;
