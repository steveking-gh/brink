// __FIRMION_VERSION_STRING writes the tool version into the output.
// Also verifies it can be used in a print statement.
section image {
    print "Firmion version: ", __FIRMION_VERSION_STRING, "\n";
    wrs __FIRMION_VERSION_STRING;
}

output image;
