// Const expression depends on addr(), which requires engine-time addressing.
// Const values must be resolvable before the engine runs.  Expected: ERR_85.
section foo {
    wr8 1;
}
const ADDR = addr(foo);
section bar {
    wr64 ADDR;
}
output bar;
