// Const expression depends on abs(), which requires engine-time addressing.
// Const values must be resolvable before the engine runs.  Expected: IRDB_19.
section foo {
    wr8 1;
}
const ADDR = abs(foo);
section bar {
    wr64 ADDR;
}
output bar;
