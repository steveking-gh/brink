section bar {
    wrs "bar";
    // should fail since bar isn't in this section
    assert sec_offset(boo) == 3;
}

// top level section
section foo {
    set_addr 0x1000;
    wr boo;
}

// section after reference
section boo {
    wrs "boo";
    wr bar;
}

output foo;