section bar {
    wrs "bar";
}

// top level section
section foo {
    set_addr 0x1000;
    wr bar;
    wr bar;

    // Ambiguous reference, should fail
    assert addr_offset(bar) == 3;
}

output foo;