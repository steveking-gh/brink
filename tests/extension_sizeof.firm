// Tests sizeof() for both section names and extension names.
// sizeof(top) returns the section byte size; sizeof(firmion::test_crc) returns
// the extension's fixed byte footprint — both must agree on 4 bytes here.
section top {
    wr firmion::test_crc(0xAABBCCDD);
    assert sizeof(top) == 4;
    assert sizeof(firmion::test_crc) == 4;
}

output top;
