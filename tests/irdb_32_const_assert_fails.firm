// ERR_98: assert expression evaluates to false inside a const if/else body.
// The assert 0; in the taken then-branch must fire ERR_98 at const-resolution time.

if 1 {
    assert 0;
}

section foo {
    wr8 1;
}

output foo;
