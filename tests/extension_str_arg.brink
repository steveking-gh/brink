// Passes a string const as an extension argument.
// IRDB accepts the string; brink::test_crc rejects it at runtime with ERR_167
// because it expects an Int arg.
const LABEL = "hello";
section top {
    wr brink::test_crc(LABEL);
}
output top;
