// ERR_100: sizeof() applied to a namespace-qualified name not in the extension registry.
// firmion::no_such_ext is not registered — ERR_100 must fire during SizeofExt validation.

section foo {
    assert sizeof(firmion::no_such_ext) == 0;
}

output foo;
