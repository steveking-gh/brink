// Region name conflicts with an existing const name.
// ERR_58: "FLASH" is already declared as a const.
const FLASH = 0x08000000;
region FLASH {
    addr = 0x1000;
    size = 0x100;
}
section top {
    wr8 0x00;
}
output top;
