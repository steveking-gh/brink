// Section-level if/else: condition is true, so then-branch wr8 is emitted.
const ENABLED = 1;

section foo {
    if ENABLED > 0 {
        wr8 0x41;  // 'A'
    } else {
        wr8 0x42;  // 'B'
    }
}

output foo;
