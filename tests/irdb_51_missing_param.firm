// Named argument call to firmion::test_sum8 with only 'h' supplied.
// Parameters 'a' through 'g' are absent; IRDb emits ERR_107 for each.
section top {
    wr firmion::test_sum8(h=8);
}
output top;
