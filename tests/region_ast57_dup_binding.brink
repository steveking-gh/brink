// Two sections bound to the same region.
// ERR_52: only one section may bind to a given region.
region FLASH {
    addr = 0x1000;
    size = 0x100;
}
section first in FLASH {
    wr8 0x01;
}
section second in FLASH {
    wr8 0x02;
}
output first;
