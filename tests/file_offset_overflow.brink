// file_offset_overflow.brink
//
// pad_file_offset with a target less than the current file_offset must
// produce ERR_146 (location counter cannot move backwards).

section foo {
    wrs "Hello";                // file_offset() is now 5
    pad_file_offset 3;          // backwards: 3 < 5 → error
}

output foo;
