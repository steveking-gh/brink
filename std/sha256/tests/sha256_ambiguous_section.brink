// Section 'data' is written twice by the parent, so the section-name form
// is ambiguous.  Expects EXEC_56.
section data {
    wr8 0x01; wr8 0x02; wr8 0x03; wr8 0x04;
}
section top {
    wr data;
    wr data;
    wr std::sha256(data);
}
output top;
